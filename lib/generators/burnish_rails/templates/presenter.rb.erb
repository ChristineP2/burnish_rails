<% module_namespacing do %>
class <%= "#{class_name}Presenter" %> < ApplicationPresenter
  include BurnishRails::Presenter.of('<%= class_name.underscore %>',
               '<%= class_name.split('_').map(&:first).join.downcase %>',
                <%= class_name %>)

  # def configure(...)
  #   # Hook for additional config
  # end
end
<% end %>
